function laCajaDePandora(num){
    if (num % 2 === 0) {
        // Si es par, se convierte a binario
        return num.toString(2);
      } else {
        // Si es impar, se convierte a hexadecimal
        return num.toString(16);
      }
    }

    function Franco (){
        return {
            nombre:"Franco",
            edad:20,
            nacionalidad:"Argentina"
        }
    }